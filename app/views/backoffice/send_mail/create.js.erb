$(function(){
  $('#sendMailModal').modal('hide'); // Esconde o modal

  $('#subject-text').val(''); // Reseta o valor do campo
  $('#message-text').val(''); // Reseta o valor do campo

  $.notify('<%= @notify_message %>', '<%= @notify_flag %>') // Mostra a mensagem de sucesso
});
